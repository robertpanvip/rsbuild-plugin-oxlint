name: Release

on:
  push:
    branches:
      - main
      - master

concurrency: ${{ github.workflow }}-${{ github.ref }}

jobs:
  release:
    name: Release
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Repo
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
          registry-url: 'https://registry.npmjs.org'

      - name: Setup pnpm
        uses: pnpm/action-setup@v3
        with:
          version: 9

      - name: Install Dependencies
        run: pnpm install

      - name: Detect Changed Packages
        id: changed
        run: |
          git fetch origin main
          CHANGED=$(git diff --name-only origin/main...HEAD | grep -E '^packages/([^/]+)' | sed 's|packages/||' | cut -d'/' -f1 | sort -u | tr '\n' ' ' | tr -d ' ')
          echo "changed_packages=$CHANGED" >> $GITHUB_OUTPUT
          echo "Changed packages: $CHANGED"

      - name: Build Changed Packages
        if: steps.changed.outputs.changed_packages != ''
        run: |
          for pkg in ${{ steps.changed.outputs.changed_packages }}; do
            echo "Building package: $pkg"
            pnpm --filter "rsbuild-plugin-$pkg" build
          done

      - name: Publish Changed Packages
        if: steps.changed.outputs.changed_packages != ''
        run: |
          for pkg in ${{ steps.changed.outputs.changed_packages }}; do
            echo "Publishing package: $pkg"
            cd packages/$pkg
            npm version patch --no-git-tag-version
            npm publish --access public --registry https://registry.npmjs.org/
            cd ../..
          done
        env:
          NPM_TOKEN: ${{ secrets.NPM_TOKEN }}

      - name: Commit and Push Version Changes
        if: steps.changed.outputs.changed_packages != ''
        run: |
          git config --local user.email "github-actions[bot]@users.noreply.github.com"
          git config --local user.name "github-actions[bot]"
          git add packages/*/package.json
          git commit -m "chore: bump package versions" || echo "No changes to commit"
          git push
